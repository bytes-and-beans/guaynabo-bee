{# returns pre-rendered HTML for a week of the events calendar
 #}
{% apply spaceless %}
{% from "_partials/frontpageNews/newsWidget.twig" import week %}
{% set offset = craft.app.request.getParam('offset') ?? 0 %}
{{week(offset)}}
{% endapply %}